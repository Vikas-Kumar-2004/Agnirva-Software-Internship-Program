function calculateAverage(numbers) {
  let total = 0;
  for (let num of numbers) {
    total += num;
  }
  let average = total / numbers.length;
  return averge; // typo
}

let nums = [10, 20, 30, 40];
console.log("Average is", calculate_avg(nums)); // wrong function name
